# Copy this file to terraform.tfvars and fill in real values.
# terraform.tfvars itself is gitignored and never committed.

minio_root_password = "change-me-to-something-real"
airflow_postgres_password = "change-me-too"
airflow_fernet_key = "paste the generated fernet key"
airflow_admin_password = "create a password for airflow"